Some programmers implement APIs that take numerical IDs as parameter and do not make any checks if the user is permitted to see that object.

Find the URL for accessing the documents and iterate the IDs by incrementing the number. You should not need more than 10 tries.